# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
firebase-debug.log*
firebase-debug.*.log*

# Firebase cache
.firebase/

# Firebase config

# Uncomment this if you'd like others to create their own Firebase project.
# For a team working on the same Firebase project(s), it is recommended to leave
# it commented so all members can deploy to the same project(s) in .firebaserc.
# .firebaserc

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Service account keys (SECURITY: never commit credentials)
service-account.json
**/service-account.json

# Vestige memory exports (contains credentials, share manually)
bill-vestige-export.db
*vestige-export.db

# Bertie onboarding files (share manually, may contain notes/credentials)
BERTIE-MEMORY-DUMP.json
BERTIE-ONBOARDING.md
blackstart/

# API keys and secrets (SECURITY: never commit credentials)
*.api-key.txt
.api-key.txt
**/.api-key.txt

# dataconnect generated files
.dataconnect
app/email/

# WhatsApp Session Data (CRITICAL)
whatsapp-worker/.wwebjs_auth
whatsapp-worker/.wwebjs_cache
.wwebjs_auth
.wwebjs_cache

# Local one-off / operational scripts holding real PII (cyber.md M-2b landmine).
# The repo is PUBLIC — these untracked files (_send-reinvite-emails.js, _pst-state.json with
# real player emails + internal Exchange hostnames) must never be accidentally `git add`ed.
scripts/_*
**/scripts/_*
scripts/*-state.json
