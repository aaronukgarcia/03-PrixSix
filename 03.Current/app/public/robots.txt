# Prix Six — members-only app
# Default-deny: all routes blocked except the two public pages.
# Any new route added is automatically protected without a robots.txt update.
# Note: Disallow is advisory — authentication enforces actual access control.

User-agent: *
Allow: /login
Allow: /about
Disallow: /
